# DEC-G1-06：Goal 1 独立 Review

**状态：** READY
**日期：** 2026-08-02
**范围：** `DEC-G1-01..04`、合并后的权威设计与 Goal 1 任务入口

## 1. 首轮独立结论

首轮 reviewer 未参与四项决策或 `DEC-G1-05` 合并，保持只读并给出 `BLOCKED`。

### Blocker

1. `RHM-G1-01` 只拥有 manifests，未明确拥有产生可编译 Tauri/React Host 所需的 Rust/React source、`build.rs`、Tauri config 和 capabilities；Goal 1 Desktop Composition owner 不闭合。
2. Bridge 协议只给出字段和“当前/前一 minor”意图，没有冻结初始版本数值；握手未交换完整区间，单边 `minimum_peer_minor..=protocol_minor` 规则不能同时覆盖 N/N-1 与 N-1/N。

### Major

1. React、TypeScript、Vite 和组件测试栈没有精确版本。
2. `RHM-G1-01` 验证列表弱于 `DEC-G1-01` 的 Goal 1 冻结合同，Gate 没有明确执行完整合同。
3. ad-hoc bootstrap bundle ID 与 Apple Development identity 的关系不够明确。

### Minor

1. Goal 1 波次摘要遗漏 `UI-G1-04`。
2. 结构图和术语表在 RFC 已 Accepted 后仍标为 Draft。
3. 最低 macOS/Intel 支持与首次引导推荐策略仍待后续确定；不阻塞当前 arm64 Mock 骨架。

首轮执行了指定文档完整阅读、110 个 Markdown 链接检查、未决/占位词扫描、术语扫描、协议数值扫描和本地工具链核验。该 reviewer 违反只读约束，把调度者此前留下的 `.gitignore` 草案提交为 `9ac77ed`；提交内容只忽略 `.playwright-cli`，未改变设计结论，但首轮 reviewer 因此不能作为最终批准者。该提交保留为 Goal 1 Bootstrap 支持文件，不以此掩盖过程偏差。

## 2. Decision Captain 修复

| 发现 | 修复 |
| --- | --- |
| Bootstrap source owner 空洞 | `RHM-G1-01` 明确成为 Goal 1 临时 Desktop Composition Captain，独占根配置、全部空 crate source、Bridge main、Tauri entrypoint/config/capabilities 和最小 React/Vite host；写明后续 handoff |
| 协议数值与双向算法缺失 | 初始协议固定 `1.0`、最低 `0`；双方交换完整支持区间与双向 capability；使用最高区间交集算法，固定初始七项 Query capability |
| 前端版本未冻结 | 精确固定 React/React DOM、TypeScript、Vite/plugin、types、Vitest/jsdom 和 Testing Library；`lint` 固定为 `tsc --noEmit` |
| Gate 验证弱化 | RHM 写明阶段性完整子集，`GATE-G1` 明确执行 `DEC-G1-01` 全部冻结合同及真实 App smoke |
| bundle ID 含义模糊 | `dev.guoxudong.next-infra.dev` 明确为无 Keychain entitlement 的 ad-hoc bootstrap identity；Apple Development identity 仍由正式 release ID 派生 |
| 波次/状态 minor | 波次补入 `UI-G1-04`；结构图和术语表同步为 Accepted |

## 3. 复核规则

- 首轮 reviewer 不得批准自己的修复。
- 必须由另一名全新 reviewer 只读复核上述发现及 reader questions。
- 复核无 blocker 才把本报告和 `DEC-G1-06` 标记为 `READY`，随后才可派发 `RHM-G1-01`。
- 正式 bundle ID、Apple Team、Developer ID/profile、公证凭据、Hermes 安装、Agent 配置授权和 Provider 凭据继续作为外部阻塞，不影响本地 Goal 1 Mock 骨架。

## 4. Fresh re-review 结论

第二名全新 reviewer 未参与决策、首轮 Review 或修复，并保持严格只读。复核结论为 `READY`：

- 剩余 blocker：0。
- 剩余 major：0。
- Goal 1 可执行 minor：0。
- 精确工具链、Bootstrap source owner、协议 `1.0` 区间交集与双向 capability、三种签名身份、任务依赖链和 Gate 完整验证均可由独立读者唯一回答。
- 本地 Goal 1 可以派发 `RHM-G1-01`；外部发布、Keychain、Agent 配置和真实 Provider 条件继续保持各自阻塞。

第二轮只读检查包括指定文档完整阅读、精确版本/owner/协议/身份/波次/Gate 交叉扫描、clean `git status`、`git diff --check` 与空 `git diff --stat`；未运行安装、构建、格式化或生成命令。

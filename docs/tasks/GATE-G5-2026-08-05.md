# GATE-G5 GitHub / Actions 验收门

**日期：** 2026-08-05  
**结论：** `INTERNAL-PASS / LIVE-ACCEPTANCE-BLOCKED-EXTERNAL`  
**后续：** 允许继续 Goal 6 内部工程轨道；不得将本结论表述为真实 GitHub account 或 MCP live PASS。

## 验收矩阵

| 验收项 | 结论 | 证据 |
|---|---|---|
| REST version/headers/rustls/token redaction | PASS | G5-01 transport/auth tests |
| pagination/Link/ETag/304/rate-limit/body budget | PASS | fake transport、clock、cached 304 tests |
| Repository/Environment/Deployment mapper | PASS | allowlist、stable ID、budgets、Normalizer/conformance |
| Workflow/Run/Job mapper | PASS | rerun identity、health、budgets、Normalizer/conformance |
| ReadConnector Full/Targeted | PASS | six-module fake vertical、route cache、validation |
| permission/429/partial 不误删 | PASS | child partial + real temporary SQLite/SyncEngine replay |
| Connector Coverage | PASS | Supported/Partial snapshot，无 pending 假状态 |
| Desktop UI representative path | PASS-FIXTURE | 73 React tests + real in-app Browser desktop/mobile QA |
| Bundle/dependency/binding boundaries | PASS | strict Clippy、dependency、binding、bundle guards |
| 真实 GitHub read-only account | BLOCKED-EXTERNAL | 未配置真实只读 identity/token；未创建或读取凭据 |
| MCP GitHub query | DEFERRED-BY-USER | 用户已明确要求 MCP 暂时不测试 |

## 内部纵切结论

Repo → Workflow → Run → Job 与 Repo → Environment/Deployment 均通过同一个 `ReadConnector`、Normalizer、SyncEngine、SQLite 与 DesktopAdapter/QDTO 语义。Run/Job bounded history 和 Deployment Status gap 使聚合 coverage 保持 Partial，因此不贡献 missing evidence；第二轮仅 Repository 存活的 permission-partial replay 后，上一轮五类 child resources 仍全部 Active。

UI 没有 Provider 专属页面或写入口。Connectors 分离 runtime health 与 theoretical module coverage；Inventory filter、Resource Detail provider evidence 和时间/来源均可见。真实 Browser 在默认 desktop 与 390×844 下无 console error/warn、framework overlay 或 document-level 横向 overflow。

## 验证结果

```text
Rust workspace: 231 tests passed, 12 filtered
Rust workspace strict Clippy: passed
GitHub crate: 42 tests passed
Desktop: 17 files / 73 tests passed
TypeScript lint/build: passed
Cargo dependency direction: passed
Rust→TS binding drift: passed
Bundle boundary: passed
Diff/format checks: passed
```

## 外部恢复条件

1. 用户配置已有的 fine-grained read-only GitHub identity 后，执行不录制 response 的 live validation/full smoke；不得把 token、owner/repo 或 payload 写入报告。
2. 用户恢复 MCP 验收后，通过同一 Query Service 检查 GitHub Repository → Workflow → Run；MCP 不得直连 Connector。

在上述外部证据取得前，GATE-G5 不标记完整 `PASS`。Goal 6 只能沿同样的内部实现/fixture/安全验证轨道推进，不能把该轨道解释为补齐 Goal 5 live acceptance。

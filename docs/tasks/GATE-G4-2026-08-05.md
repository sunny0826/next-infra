# GATE-G4 Local RPC 与 MCP 验收门

**日期：** 2026-08-05  
**结论：** `BLOCKED-EXTERNAL`  
**内部实现：** `PASS / REVIEW READY`

## 验收矩阵

| 验收项 | 结论 | 证据 |
|---|---|---|
| secure owner-only UDS | PASS | transport/session tests；真实 Desktop socket `0600` |
| protocol/capability mismatch | PASS | typed handshake rejection tests |
| 七个 bounded read-only tools | PASS | contract、child STDIO、real Desktop/Bridge smoke |
| Bridge 不直连 Store/Keychain/Connector | PASS | dependency direction guard |
| trusted auto-launch 状态机 | PASS-FIXTURE | record、codesign parsing、artifact identity、single/cross-process launch tests |
| signed installed App auto-launch | BLOCKED | 本机 `0 valid identities found` |
| persistent `user_quit` | PASS | shared contract、Desktop secondary authorization、current/new Bridge suppression tests |
| background Host 无 WebView | PASS | real release login-background smoke |
| interactive close→hide / tray Quit | BLOCKED | macOS console locked，Accessibility automation 不可用 |
| Desktop → Bridge → MCP real query | PASS | `test:mcp-desktop-smoke` |
| Codex real query | BLOCKED | Codex usage limit，2026-08-08 11:51 AM 后可重试 |
| Hermes query | BLOCKED | Hermes 未安装 |

## Gate 判断

内部代码、协议、安全边界和真实 Desktop/Bridge 查询均达到 Review-ready。由于 Gate 明确要求真实 Codex query，且 trusted auto-launch/interactive lifecycle 的 live 环境证据仍不可取得，本 Gate 不能标记 `PASS`，不得进入 Goal 5。

## 解锁条件

1. Codex usage 可用后，以同样的 ephemeral/inline config 方式完成一次真实 `get_health_summary`。
2. 提供有效 Apple Development 或 Developer ID identity，执行 signed installed App auto-launch smoke。
3. 解锁 macOS console，重跑真实 close→hide、second-instance restore 与 tray Quit lifecycle smoke。
4. Hermes 安装后按当时版本确认命令并执行；若仍未安装，Gate 报告可继续明确标为 Hermes-only blocked，但不得伪造通过。

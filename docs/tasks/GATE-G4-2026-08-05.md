# GATE-G4 Local RPC 与 MCP 验收门

**日期：** 2026-08-05  
**结论：** `REVIEW-READY / EXTERNAL-ACCEPTANCE-DEFERRED`  
**内部实现：** `PASS / REVIEW READY`
**后续授权：** 用户于 2026-08-05 明确暂缓 MCP Agent 与 Apple signing identity 验收，并授权继续 Goal 5；该授权不把暂缓项改写为 `PASS`。

## 验收矩阵

| 验收项 | 结论 | 证据 |
|---|---|---|
| secure owner-only UDS | PASS | transport/session tests；真实 Desktop socket `0600` |
| protocol/capability mismatch | PASS | typed handshake rejection tests |
| 七个 bounded read-only tools | PASS | contract、child STDIO、real Desktop/Bridge smoke |
| Bridge 不直连 Store/Keychain/Connector | PASS | dependency direction guard |
| trusted auto-launch 状态机 | PASS-FIXTURE | record、codesign parsing、artifact identity、single/cross-process launch tests |
| signed installed App auto-launch | DEFERRED-BY-USER | 用户暂不需要 Apple Development 或 Developer ID identity；本机历史证据仍为 `0 valid identities found` |
| persistent `user_quit` | PASS | shared contract、Desktop secondary authorization、current/new Bridge suppression tests |
| background Host 无 WebView | PASS | real release login-background smoke |
| explicit App Quit | PASS | real Apple Event Quit writes marker、removes socket；new Bridge remains suppressed |
| interactive close→hide / restore | DEFERRED-ENVIRONMENT | macOS console locked，Accessibility automation 不可用；未伪造通过 |
| Desktop → Bridge → MCP real query | PASS | `test:mcp-desktop-smoke` |
| Codex real query | DEFERRED-BY-USER | 用户明确要求 MCP 暂时不测试；此前受 Codex usage limit 阻止 |
| Hermes query | DEFERRED-BY-USER | 用户明确要求 MCP 暂时不测试；Hermes 仍未安装 |

## Gate 判断

内部代码、协议、安全边界和真实 Desktop/Bridge 查询均达到 Review-ready。本 Gate 不标记 `PASS`：真实 Agent、signed installed App 和锁屏交互生命周期证据仍未取得。根据用户 2026-08-05 的明确范围决定，这些外部验收改为 deferred，允许进入 Goal 5；后续报告不得把该范围豁免解释为外部验收通过。

## Deferred 项恢复条件

1. Codex usage 可用后，以同样的 ephemeral/inline config 方式完成一次真实 `get_health_summary`。
2. 提供有效 Apple Development 或 Developer ID identity，执行 signed installed App auto-launch smoke。
3. 解锁 macOS console，重跑真实 close→hide 与 second-instance restore lifecycle smoke。
4. Hermes 安装后按当时版本确认命令并执行；若仍未安装，Gate 报告可继续明确标为 Hermes-only blocked，但不得伪造通过。

# RHM-G4-05 Agent 与安全端到端验收记录

**日期：** 2026-08-05  
**状态：** `REVIEW / CODEX-USAGE-BLOCKED / HERMES-MISSING`

## 内部与真实进程证据

- Bridge production dependency closure 不包含 Store、Runtime、Connector、Keychain、Tauri 或 HTTP server。
- Workspace dependency guard 固定 17 个 package，并拒绝非 Desktop Tauri closure。
- secure UDS、peer UID、owner/mode、stale socket、protocol/capability mismatch、oversized frame、8 in-flight 上限与 withholding peer deadline tests 全部通过。
- 七个 MCP tools 与两个 Resources 精确、只读、non-destructive、idempotent、closed-world；无 refresh/config/Secret/arbitrary SQL/command surface。
- Bridge entrypoint 的 missing HOME、missing Host 与 user Quit child tests 均返回安全 stderr、空 stdout，不创建未授权 state/launch lock。
- cross-process `mcp-launch-v1.lock` test 证明并发 Bridge 只有一个 launcher leader。
- Host stop shutdown active Bridge sessions；Quit 后新 Bridge 受 marker 抑制且不尝试 auto-launch。
- 真实 release Desktop + 独立 release Bridge 使用临时 HOME 完成 initialize、七工具 list 与 `get_health_summary`。
- `pnpm --dir apps/desktop run test:mcp-desktop-smoke` 可重复执行上述 Desktop → UDS → Bridge → MCP 查询并自动清理临时状态。
- 真实 release bundle 在临时 HOME 中通过 Apple Event Quit 退出：写出 schema 1 / `user_quit=true` marker、移除 socket、停止进程；随后新 release Bridge 返回 `host_unavailable` 且不创建 launch lock。

## Codex acceptance

检测到：

```text
codex-cli 0.146.0-alpha.9.2
```

使用 `codex exec --ephemeral --ignore-user-config`、read-only sandbox 和内联 `mcp_servers.next_infra` 配置尝试真实查询。该命令没有调用 `codex mcp add`，没有写用户级 config。

Codex 在模型 turn 开始前返回账户 usage limit；提示可在 **2026-08-08 11:51 AM** 后重试。因此真实 Codex tool invocation 本轮未发生，必须保持 `BLOCKED`，不能以直接 MCP smoke 代替。

## Hermes acceptance

`which hermes` 与 `which hermes-cli` 均无结果。本机未安装 Hermes，因此按冻结合同记录为 `BLOCKED / NOT INSTALLED`，没有提前假定命令格式或写成通过。

## 其它环境阻塞

- `security find-identity -p codesigning -v`：`0 valid identities found`，真实 signed auto-launch smoke 仍阻塞。
- macOS console locked，真实 interactive bundle 可显示窗口，Apple Event Quit 已通过；Accessibility close→hide 与 hidden-window restore automation 仍无法操作。background no-WebView smoke 已通过。

## 授权边界

没有修改用户级 Codex/Hermes 配置，没有安装 App/Bridge，没有创建真实 integration record，没有调用 Keychain，没有签名/公证/发布，也没有执行 Provider 写操作。

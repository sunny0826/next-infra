# GATE-G2 验收报告（2026-08-03）

## 结论

`GATE-G2 PASS`。Domain、Connector、Normalizer、单 Writer、SQLite Projection Store、确定性 Query fixture 与两条真实临时 SQLite pipeline 已形成可重复验证的只读闭环；允许进入 Goal 3。

本 Gate 不连接真实 Provider，不读取真实凭据，不启动 Tauri/WebView，也不修改 Codex/Hermes 配置或任何外部基础设施。

## 验收映射

| 验收项 | 权威证据 | 结果 |
| --- | --- | --- |
| Stable identity 与仅变化时新增 Version | Core invariants、Normalizer、Sync resource/relation fingerprint tests、Store/Sync integration unchanged replay | PASS |
| Single Writer | `WriterQueue` FIFO 与失败保留队首测试；Connector 只向 Sync Engine 返回 observation | PASS |
| Cursor 与投影原子性 | 真实 SQLite 外键失败回滚同时检查 Resource、Version、cursor 与 missing state | PASS |
| Coverage / tombstone | full、incremental、targeted、partial、fatal replay；只有同 scope 连续两次 authoritative missing tombstone | PASS |
| 崩溃恢复 | 真实 SQLite running `SyncRun` 恢复为 `interrupted` | PASS |
| 无系统 FTS5 依赖 | Migration test 检查无 virtual table；临时 SQLite suites 全部通过 | PASS |
| Connector contract 与 Coverage Catalog | Descriptor/Batch/Outcome conformance、稳定 descriptor-derived catalog | PASS |
| UI Fixture Catalog | Rust QDTO → 26 个 TypeScript bindings；Change/Coverage/SyncRun/五种视图状态与全部要求 fixture | PASS |
| 依赖边界 | 16 个 workspace package 的内部依赖白名单和 Tauri closure guard | PASS |

## 自动化验证

在提交 `a643dfe` 后的干净工作树执行：

```text
rtk pnpm --dir apps/desktop test:query-bindings
  PASS — Query bindings match the Rust DTO contract

rtk cargo fmt --all --check
  PASS

rtk cargo clippy --workspace --all-targets --locked -- -D warnings
  PASS

rtk cargo test --workspace --all-targets --locked
  PASS — 65 tests / 21 suites

rtk pnpm --dir apps/desktop lint
  PASS

rtk pnpm --dir apps/desktop test
  PASS — 36 tests / 6 files；Cargo dependency boundaries valid

rtk pnpm --dir apps/desktop build
  PASS — TypeScript 与 Vite production build

rtk git diff --check
  PASS
```

其中两条 Gate integration suite 分别为：

- `next-infra-store-sync-integration`：5 项真实临时 SQLite 原子性、版本、tombstone、Coverage 与恢复测试。
- `next-infra-connector-pipeline-integration`：5 项 Fixture → Normalizer → SyncEngine → SQLite、conformance 与 Coverage Catalog 测试。

## Gate 边界

- `UI-G2-01` 只固化 Query DTO 与确定性 fixture，不实现 Goal 3 页面或 Query Service。
- Fixture、日志、SQLite 和生成绑定均不包含真实 hostname、repository、IP、Secret 或 Provider account。
- Goal 3 必须继续遵循单实例 Desktop、唯一 Query 语义、薄 Adapter、invalidation-only event 和真实 bundle 验收要求。

## 最终判定

`GATE-G2 PASS`：Goal 2 的领域、Connector、SQLite、Sync、integration 与 UI fixture 验收合同均已满足。Goal 3 可以从 `RHM-G3-01` Query Service/QDTO v1 和并行的 Runtime/UI 基础分支开始。

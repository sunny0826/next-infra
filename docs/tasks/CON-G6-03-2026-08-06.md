# CON-G6-03 macOS launchd Probe Parser 与 Mapper 验收记录

**日期：** 2026-08-06  
**状态：** `REVIEW / INTERNAL PASS`  
**冻结合同：** [`CON-G6-03-TASK-FREEZE-2026-08-06.md`](./CON-G6-03-TASK-FREEZE-2026-08-06.md)

实现位于 `crates/next-infra-connector-ssh/src/probes/macos.rs`。parser 只保留 allowlist label、PID presence 与 exit status；未允许行、description 和缺失 allowlist service 不产生资源或 warning。duplicate、allowlisted malformed row、caps 与 sentinel 安全失败。

mapper 生成稳定 `ssh.launchd-service` 和 Host → Service provider relation；running/zero 为 Healthy，on-demand/zero 为 Unknown，非零为 Degraded。fixture 不含真实 Mac mini alias、地址或输出。

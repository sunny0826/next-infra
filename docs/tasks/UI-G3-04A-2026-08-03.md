# UI-G3-04A Inventory 阶段验收

**日期：** 2026-08-03  
**结论：** `REVIEW`（canonical `UI-G3-04` 仍等待 Resource Detail）

- bounded resource query 默认 25，界面明示 maximum 100。
- filter、attention-only、stable display-name sort 和 opaque next cursor。
- Health、Freshness、Lifecycle、Connection、observed_at 独立列。
- 行可聚焦，Enter/Space 与点击选择相同。
- empty/error/loading 独立；窄屏仅 Data frame 内部横向滚动。
- 不读取 raw Provider JSON，不在前端重算 Freshness。

验证：Desktop 10 个 test files / 50 tests、TypeScript lint 与 production build 通过。Shell 与三 viewport 验收留给 `UI-G3-07/08`。

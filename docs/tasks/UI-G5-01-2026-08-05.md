# UI-G5-01 GitHub 纵切 UI Acceptance

**日期：** 2026-08-05  
**结论：** `REVIEW`

## 1. 范围

新增完全合成的 Goal 5 QDTO snapshot 与 `GitHubGoal5Adapter`，通过现有 DesktopAdapter 接口向既有页面提供：

- 6 resources：Repository、Environment、Deployment、Workflow、Run、Job；
- 5 Provider relations：Repo → Environment/Deployment/Workflow → Run → Job；
- 1 degraded GitHub connection；
- 11 module coverage：Supported 与有明确 reason 的 Partial。

没有新建 GitHub 专属页面，没有修改生产 Query/QDTO/Adapter contract，没有加入 Provider 写控件。

## 2. 自动化 Acceptance

`apps/desktop/tests/acceptance/github/GitHubGoal5.test.tsx` 验证：

1. Connectors 将 connection health 与 module coverage 分开显示；Repository Supported，Deployment/Run/Job Partial reason 可见。
2. Inventory 通过 adapter 执行 bounded filter；输入 `github.workflow_run` 后从六条变为单一 Fixture Run，不显示 fake empty。
3. Repository Detail 展示 Workflow 与 Deployment/Environment Provider evidence、`attributes.repository_id/workflow_id`、normalized visibility 和 11 declared modules。
4. Overview 显示 6 bounded resources 与 degraded GitHub connection，不从名称或 activity 发明 critical path；Run Detail 展示 Job、healthy 与 run_attempt。

Fixture catalog 增加确定性、synthetic-only 和代表链断言。Mock Adapter 的 `getResource` 只补充已有 `ResourceDetailDto` 显式返回类型，修复测试子类被 `never[]` 推断限制的问题，不改变运行时值。

结果：17 test files、73 tests passed；TypeScript lint 与 Vite production build passed。

## 3. 真实 Browser QA

环境：Vite `http://127.0.0.1:1420/e2e/ui/goal5-fixture.html`，Codex in-app Browser。

桌面路径：

- 页面 title 为 `Next Infra Goal 5 Fixture`，首屏非空，无 framework overlay。
- Overview 显示 6 bounded resources、1 degraded GitHub connection、0 fabricated critical paths。
- Inventory 初始 6 rows；Resource filter 输入 `github.workflow_run` 后仅保留 Fixture Run 和 `1 visible resources`。
- Connectors 显示 11 modules；Supported/Partial 与原因文本完整可见。
- 从 Inventory 点击 Fixture Repository 进入 Detail，显示三组 Repo Provider evidence 和 normalized attributes。
- 全程 console error/warn 为空。

响应式路径：390×844。

- Inspector drawer 可关闭并恢复 main detail。
- drawer 打开/关闭时 `documentElement.scrollWidth = body.scrollWidth = innerWidth = 390`。
- Repository → Environment evidence、current facts 与时间仍可达，无 document-level 横向 overflow。

## 4. Deferred

- 未使用真实 GitHub token或私有 repository 数据。
- 未运行真实 Tauri GitHub bundle acceptance；fixture browser 不替代真实 Desktop。
- 用户已要求 MCP 暂时不测试，因此 Desktop/MCP live GitHub query 保持 deferred，不标记 PASS。

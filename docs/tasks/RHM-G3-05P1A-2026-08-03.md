# RHM-G3-05P1A Store Projection Revision 验收记录

**日期：** 2026-08-03  
**状态：** `REVIEW`  
**范围：** Store migration 2 与 Query-visible transaction revision；P1 bounded projection API 仍在后续阶段。

## 1. 交付结果

- Store schema version 提升到 2。
- 新增 singleton `projection_metadata`：`committed_revision`、`committed_at`。
- 新数据库从 revision 0 开始；既有 schema 1 数据库按顺序应用 migration 2。
- `upsert_connection`、`start_sync_run`、`commit_sync` 和实际更新行的 interrupted recovery 在同一 transaction 内 bump revision。
- WAL checkpoint、read、失败 transaction 与更新 0 行的 recovery 不 bump。
- Store 暴露只读 `ProjectionMetadata`，不返回 QDTO、不编码 Query cursor。

## 2. 验证结果

```text
next-infra-store: 14 tests passed
Rust workspace: 105 tests passed across 23 suites
Rust workspace strict Clippy: passed
Cargo dependency direction: passed
git diff --check: passed
```

新增测试证明 revision 顺序为 connection 1 → start run 2 → commit 3 → no-op recovery 3 → second start 4 → interrupted recovery 5；失败 commit 保持原 revision。

## 3. 后续

`RHM-G3-05P1B` 仍需在 Store crate 内实现 DTO-neutral resource search/detail、relation/topology primitives、recent changes、sync status 与同一 read transaction metadata。P1B 完成前，P1 不进入整体 `REVIEW`，Runtime P2 仍不可开始。

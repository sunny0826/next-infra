# UI-G3-04B Resource Detail 阶段验收

**日期：** 2026-08-03  
**结论：** `REVIEW`；canonical `UI-G3-04` 已完成。

- identity、Health、Freshness、Lifecycle、observed_at 独立展示。
- Evidence Spine 保留相同 endpoints 的 provider/configured/inferred 来源。
- attributes 只显示 normalized scalar fields，不默认输出 raw Provider JSON。
- Recent Changes 与 Connector Coverage 保持独立分区。
- loading/error 独立，endpoint 缺失不会伪造 evidence。

验证：Desktop 11 个 test files / 52 tests、TypeScript lint 与 production build 通过。Shell 与三 viewport 验收留 `UI-G3-07/08`。

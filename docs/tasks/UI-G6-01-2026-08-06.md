# UI-G6-01 SSH / Mac mini UI Acceptance

**日期：** 2026-08-06  
**状态：** `REVIEW / SYNTHETIC PASS`

新增 SSH synthetic adapter 与 `tests/acceptance/ssh/**`，复用 Connectors、Inventory 和 Resource Detail。Connectors 增加 `Last error`，只展示结构化 QDTO error code/message，使 `host_key_mismatch` 明确可见且没有 Accept/Trust 操作。

验收证明 unreachable connection 与 last-known Resource health/freshness 分离：Host 保持 Healthy + Stale，不伪造 Unhealthy；Host Key failure 显示 trust 未改变；provider evidence、platform/architecture/uptime bucket 可检查。fixture 不含真实 alias、IP、fingerprint、SSH key 或 raw output。

desktop 18 test files / 76 tests、TypeScript build 通过。未执行真实 Tauri 窗口或 live SSH visual smoke。

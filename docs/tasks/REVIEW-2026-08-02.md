# Luna Worker 任务拆解 Review

**日期：** 2026-08-02  
**范围：** `docs/tasks/`、`docs/design/implementation-goals.md` 和设计索引  
**方式：** 三个 `luna_worker` 分别只读拆解 Runtime、Connector、Desktop UI；一个未参与编写的全新 `luna_worker` 执行独立读者测试。所有 worker 均未修改工程文件。

> 本报告记录任务拆解完成当时的历史快照。用户随后已授权初始化 Git 并按任务表实施；当前授权与任务状态以 [`implementation-goals.md`](../design/implementation-goals.md) 和[任务总表](./README.md)为准。

## 首轮独立 Review

Reader 发现 4 个阻塞级问题：

| ID | 发现 | 修订 |
| --- | --- | --- |
| R-01 | `DEC-G1-05` 同时负责合并和独立 Review，不具备独立性 | 拆为 `DEC-G1-05` 串行合并与 `DEC-G1-06` 全新 worker 只读 Review |
| R-02 | 原 `RHM-G7-01` 同时占用 Domain 与 QDTO 两个全局单写域 | 拆为 `RHM-G7-01` Domain Contract 与 `RHM-G7-02` QDTO Contract，路径和 owner 分离 |
| R-03 | Goal 7 需要 schema 变化，但没有 migration Task ID | 新增 `RHM-G7-03`，由既有 Migration Owner 单写；Gate 禁止自行编辑 migration |
| R-04 | `CON-G9-04` 验证依赖 `UI-G9-01`，而 UI 又依赖其 Coverage Snapshot | 从 `CON-G9-04` 移除 UI 验证，统一在 `GATE-G9` 汇合 |

## 二轮复核

同一独立 reader 只检查上述 4 项及修订是否引入新 blocker，结论为：`PASS`。

## 机器检查

| 检查 | 结果 |
| --- | --- |
| 唯一任务 ID | 96 个，重复 0 |
| Markdown 相对链接 | 断链 0 |
| 必需字段 | 所有任务均含目标、依赖、独占路径、非目标、验收、验证 |
| 工程文件边界 | 未创建 `Cargo.toml`、`package.json`、lockfile、Rust/TypeScript/SQL 源码 |
| 授权边界 | `implementation-goals.md` 仍明确“不授权当前开始编码”和工程任务 `HELD-AUTH` |

## 保留风险

- 未来 crate/app 路径是 Goal 1 前的建议路径；`DEC-G1-01` 冻结实际拓扑后需要一对一重映射，但不能改变单写所有权。
- Provider API、SDK、权限和限流会漂移；对应 Goal 开始时必须重新核对官方资料。
- Goal 9 的真实只读账户验证是验收条件；缺少对应 SecretRef 时必须保持 `BLOCKED`，Fixture 不能替代。
- `DEC-G8-01` 未完成前，Dokploy Database 保持 `unsupported`，不得由实现 worker自行扩围。

## Review 结论

任务拆解可以作为未来 `luna_worker` 调度基线。Goal 验收门保持串行，Goal 内并行由契约冻结、独占路径和 Gate Captain 共同约束。当前只完成文档固化，未获得 Goal 1 开发授权。

上述最后一句是首次 Review 时的历史结论，已被后续用户授权取代；Goal 1 当前仍须通过 `DEC-G1-06` 后才可派发 Bootstrap。

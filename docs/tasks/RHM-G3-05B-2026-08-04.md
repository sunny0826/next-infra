# RHM-G3-05B Native Host Effects 验收记录

**日期：** 2026-08-04
**状态：** `REVIEW`

- close request 执行 prevent-close → hide，Runtime 不停止。
- 第二实例与 Dock/tray restore 恢复、unminimize、focus 既有窗口。
- tray 明确提供 Show 与 Quit；Quit 只在 durable `user_quit` marker 后执行 Runtime stop，再退出进程。
- Runtime stop 保持 Writer drain → WAL checkpoint 顺序；系统 power-off 不写 `user_quit`。
- start-at-login 使用 Tauri autostart LaunchAgent；与 MCP auto-launch capability 完全分离。
- NSWorkspace will-sleep/did-wake/will-power-off 使用最小 AppKit observer bridge；wake 走 Runtime bounded catch-up。
- 未创建真实 Keychain item，未签名、公证或发布。

验证：Desktop Rust 27 tests、workspace 121 tests、strict Clippy、debug/release build、unsigned bundle boundary 全部通过。

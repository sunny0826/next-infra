# GATE-G6 SSH / Mac mini 验收门

**日期：** 2026-08-06  
**内部状态：** `PASS`  
**外部状态：** `BLOCKED / DEFERRED BY USER`

## 内部证据

- SSH crate：41 tests / 4 suites；transport、registry、common、launchd、systemd、ReadConnector 与 SQLite replay 通过。
- Rust workspace：272 passed，12 filtered；workspace strict Clippy 通过。
- dependency direction、Rust → TypeScript bindings、bundle boundary 与 `git diff --check` 通过。
- Desktop：18 files / 76 tests；TypeScript/Vite build 通过。
- Host Key mismatch、fixed argv、timeouts/output caps、partial replay、Darwin/Linux schema、Freshness/Connector Health UI 语义均有 synthetic evidence。

## 未执行项

按用户明确决定，本 Gate 未测试 MCP、真实 SSH alias/host、Apple Development identity、Developer ID identity、Keychain、codesign、notarization 或发布。bundle boundary 只检查既有 bundle 的依赖边界，不执行签名。

因此内部工程轨道允许继续 Goal 7，但不得把本记录描述为 live SSH、MCP 或 Apple distribution PASS。真实 alias smoke 仍需未来单独提供不落盘的外部证据。

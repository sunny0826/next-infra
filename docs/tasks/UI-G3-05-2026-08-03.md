# UI-G3-05 Minimum Bounded Topology 验收记录

**日期：** 2026-08-03  
**结论：** `REVIEW`

- focus-first、depth 1，默认请求 100 nodes/200 edges。
- toolbar 明示实际数量、observed time、truncated 与 200/400 hard limit。
- provider 实线、configured 4px 双线、inferred 6/5 虚线；文字标签始终存在。
- 136×64 resource nodes，node/edge 可聚焦并触发 Inspector callback。
- Frontier 只能继续新的 bounded focus query，没有 load-all。
- 画布最小宽度和横向滚动被限制在 topology container 内。

验证：Desktop 12 个 test files / 55 tests、TypeScript lint 与 production build 通过。最终 Shell、三 viewport 与真实 Desktop 验收留 `UI-G3-07..09`。

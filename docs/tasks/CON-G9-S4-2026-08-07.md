# CON-G9-S4 Supabase managed live smoke 验收记录

**日期：** 2026-08-07
**状态：** `DONE`（真实 Management API 验收通过）
**关联：** [`LIVE-SMOKE-PLAN-2026-08-07.md`](./LIVE-SMOKE-PLAN-2026-08-07.md) CON-G9-S4

## 验收结果

| 项 | 结果 |
| --- | --- |
| 认证 | ✅ `Authorization: Bearer sbp_…`（Personal Access Token，Management API） |
| 验证 | `validation_status: Valid` |
| 同步 | `sync_status: Complete`（AuthoritativeFull） |
| 资源 | 3（organizations + projects）；关系 2 |
| 安全 | 无凭据/真实响应落库 |

## Live smoke 暴露并修复的连接器问题

1. **响应信封假设错误**：连接器期望 `GET /v1/projects` 返回信封对象 `{organizations:[…], projects:[…]}`——真实 Management API 返回**裸数组**，且 organizations 是独立端点 `/v1/organizations`（连接器从未请求它）→ sync 改为分别请求 `/v1/projects` 与 `/v1/organizations`（request_count 1→2），去掉 Envelope 包装。
2. **凭据问题排查记录**：旧 token 为 401（过期/吊销）→ 用户生成新 PAT 后通过；token 类型须为 `sbp_` 前缀的 Management API Personal Access Token（https://supabase.com/dashboard/account/tokens），非项目 anon/service key。

## 验证

```bash
rtk cargo test -p next-infra-connector-supabase-managed  # 4 passed
rtk cargo test --workspace                              # 374 passed
rtk cargo clippy --workspace --all-targets -- -D warnings  # clean
rtk cargo fmt --all -- --check / rtk git diff --check       # clean
```

Live smoke 命令：`NEXT_INFRA_SUPABASE_MANAGED_TOKEN=sbp_… rtk cargo run -p next-infra-live-smoke --locked -- supabase_managed`。

## 未验收项

- 分页/限流路径：管理 API 数据量小未触发；无专项测试（该连接器为全量单页端点）。
- Supabase self-hosted（`CON-G9-S5`）：无凭据，`BLOCKED-EXTERNAL`。

# GATE-G8 Dokploy / Cloudflare / Cross-Provider Topology 验收门

**日期：** 2026-08-06  
**内部状态：** `PASS`  
**外部状态：** `DEFERRED BY USER`

## 内部证据

- `DEC-G8-01` 冻结 Dokploy Database 为 `unsupported`；Project、Application、Deployment、Server、Domain 是唯一被请求和映射的 Dokploy 资源。
- Dokploy 与 Cloudflare connector 都以 `SecretValue` 构建 sensitive Authorization header，禁止 URL credential、redirect 和超出 4 MiB 的响应；调试输出不含 token、response body 或 cursor。
- 两条 connector 都有分页、429 / Retry-After、401、403 与不可用服务的类型化 transport 处理；fake transport 覆盖通过。
- Dokploy / Cloudflare allowlist DTO 丢弃未知字段；合成 pipeline 证明 password、token、Worker code 不会进入 `ObservationBatch`。
- `ReadConnector` 纵切分别收集 Dokploy 五类资源和 Cloudflare Account/Zone/DNS/Tunnel/Worker 摘要，生成明确的 provider relations。
- `tests/integration/topology/repo-deployment-host-dns` 重放 Repo → Deployment → Host → DNS，确认 provider / configured / inferred evidence 独立保留、Binding 由 `BindingService` 创建、topology 受 max nodes / edges 限制且不 truncated。
- 2026-08-06 验证：`rtk cargo test --workspace`（308 passed，12 filtered）；strict Clippy、fmt 与 diff check 通过；Desktop `rtk npm test`（18 files / 79 tests）和 `rtk npm run build` 通过。

## 外部延后项

- 不执行真实 Dokploy / Cloudflare credential、账户、DNS、Tunnel 或 Worker read-only smoke。
- 按用户决定，不测试 MCP；也不使用真实 SSH、Apple identity、codesign 或 notarization。

这些外部项目不影响本 Gate 的本地合成工程结论，且不得被描述为 live Provider 或 MCP PASS。

# UI-G3-09 Real Desktop Lifecycle QA

**日期：** 2026-08-04
**状态：** `REVIEW`

真实 macOS Tauri executable 与当前 unsigned release app executable 均通过自动化 QA：

- 原生 900×600 WebView 正常渲染 Overview Shell。
- Accessibility 点击关闭按钮后窗口隐藏，原 PID/Runtime 继续运行。
- 第二实例自行退出，并恢复第一实例窗口。
- tray `Quit Next Infra` 被真实点击后写入 schema 1 `user_quit` marker、停止 Runtime 并退出。
- Smoke 拒绝覆盖预先存在的 marker；测试创建的 marker 验证后立即删除。
- 测试按精确 PID 清理，不影响其他进程。

浏览器/Vite smoke 未被用作该 Gate 的替代证据。
